// import { selectCustomFooterDomain } from '../selectors';

describe('selectCustomFooterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
