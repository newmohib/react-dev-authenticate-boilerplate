/*
 * ViewUsers Messages
 *
 * This contains all the text for the ViewUsers container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewUsers';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ViewUsers container!',
  },
});
